#include<stdio.h>
#include<string.h>
char *str(const char *s, char *old, char *new1)
  char *a;
    int i, count = 0;
    int newstr = strlen(new1);
    int oldstr = strlen(old);
 
{
{
  int a=i[10],j[10];
    for (i = 0; s[i] != '\0'; i++)    
    {
        if (str(&s[i], old) == &s[i]) 
        {
            count++;
            i += oldstr - 1;
        }
    }
     temp=s[0];
    }
    
}
int main()
{
      char str[100], c[10], d[10];
        printf("Enter a string:\n");

}

